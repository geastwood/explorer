<h4>{{title}}</h4>
<span>{{name}}</span><div data-toggle="popover" data-title="{{title}}" data-container="body"
    data-placement="right" data-content="{{{detail.markup}}}"
    class="glyphicon glyphicon-info-sign" aria-hidden="true" style="top:2px;
    padding-left: 5px;"></div>
